Hash table in C.

Implementation of a default key-value hash table.

Makefile creates static (lib_htab.a) and dynamic (lib_htab.so) libraries.

To create dynamic library uncomment makefile

Example of usage is in test.c file.

TODO: better resize system